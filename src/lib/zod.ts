import { standardSchemaResolver } from '@hookform/resolvers/standard-schema'

/** TODO: Remover isso
 * Executar o comando abaixo para instalar a nova versão do hookform/resolvers
 * `npm install @hookform/resolvers@5.1.0`
 */

export const resolver = standardSchemaResolver
